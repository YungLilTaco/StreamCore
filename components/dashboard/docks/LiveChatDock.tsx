/**
 * Barrel name for the live chat tile. Implementation lives in `ChatDock` (same UI the grid mounts).
 *
 * Twitch embed: first **Enter** in dock may show Twitch’s own confirmation dialog — expected.
 */
export { ChatDock as LiveChatDock } from "@/components/dashboard/docks/ChatDock";
